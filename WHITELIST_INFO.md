# 🛡️ Whitelist Protection System

## Что это?

**Whitelist** - это система защиты ваших существующих подписчиков и подписок от действий бота.

## 🔒 Что защищается?

Бот **НИКОГДА** не будет:

1. ✅ Подписываться на тех, на кого вы УЖЕ подписаны
2. ✅ Отписываться от ваших существующих подписок
3. ✅ Взаимодействовать с вашими текущими подписчиками
4. ✅ Трогать пользователей из custom whitelist

## 🚀 Как это работает?

### Автоматическая защита:

При **первом запуске** бот автоматически:

1. Получает список ВСЕХ ваших подписчиков
2. Получает список ВСЕХ ваших подписок
3. Сохраняет их в `whitelist.json`
4. Использует этот список для фильтрации

### Пример работы:

```
У вас есть:
- 500 подписчиков
- 300 подписок

Бот при запуске:
✅ Сохранит все 800 пользователей в whitelist
✅ Никогда не подпишется на них снова
✅ Никогда не отпишется от них
✅ Исключит их из поиска целевой аудитории
```

## 📁 Файл whitelist.json

Структура:

```json
{
  "followers": [
    "123456789",
    "987654321"
  ],
  "following": [
    "111222333",
    "444555666"
  ],
  "custom": [
    "777888999"
  ]
}
```

- **followers** - ваши подписчики (защищены)
- **following** - ваши подписки (защищены)
- **custom** - дополнительные пользователи (можно добавить вручную)

## 🔧 Ручное добавление в whitelist

Если хотите защитить конкретного пользователя:

1. Откройте `whitelist.json`
2. Добавьте user_id в раздел `custom`:

```json
{
  "custom": [
    "123456789",
    "987654321"
  ]
}
```

### Как узнать user_id?

```python
from instagrapi import Client

cl = Client()
cl.login("username", "password")

# Получить user_id по username
user_id = cl.user_id_from_username("target_username")
print(user_id)
```

## ⚠️ Важно!

### Первый запуск:

При первом запуске бот потратит **1-5 минут** на построение whitelist (зависит от количества подписчиков/подписок).

Вы увидите:
```
🛡️ Building whitelist (protecting existing followers/following)...
📥 Fetching your followers...
📤 Fetching your following...
✅ Whitelist built:
   - Followers protected: 500
   - Following protected: 300
   - Total protected: 800
```

### Последующие запуски:

Whitelist загружается из файла мгновенно:
```
🛡️ Whitelist loaded: 800 users protected
```

## 🔄 Обновление whitelist

### Когда обновлять?

Обновляйте whitelist если:
- Получили много новых подписчиков вручную
- Подписались на новых людей вручную
- Хотите защитить новые связи

### Как обновить?

**Вариант 1: Удалить файл (полное обновление)**

```bash
rm whitelist.json
# При следующем запуске бот пересоздаст whitelist
```

**Вариант 2: Через код (добавить новых)**

```python
bot = FollowerBot("username", "password")
bot.login()
bot._build_whitelist()  # Обновит whitelist
```

## 📊 Статистика фильтрации

При поиске целевой аудитории вы увидите:

```
🔍 Finding users from @competitor...
✅ Found 50 potential targets (excluded 25 whitelisted)
```

Это значит:
- Найдено 75 пользователей
- 25 из них в whitelist (пропущены)
- 50 новых целей для подписки

## 🛡️ Гарантии безопасности

### Тройная защита:

1. **При поиске целей** - whitelist исключается из результатов
2. **При подписке** - проверка перед каждой подпиской
3. **При отписке** - КРИТИЧЕСКАЯ проверка перед отпиской

### Код защиты:

```python
# Перед подпиской
if self._is_whitelisted(user_id):
    print("🛡️ User is whitelisted, skipping...")
    return False

# Перед отпиской
if self._is_whitelisted(user_id):
    print("🛡️ User is whitelisted, NEVER unfollowing!")
    return False
```

## ❓ FAQ

**Q: Бот отпишется от моих друзей?**
A: НЕТ! Все существующие подписки защищены whitelist.

**Q: Бот подпишется на моих подписчиков?**
A: НЕТ! Все подписчики в whitelist и исключаются из целей.

**Q: Что если я подпишусь на кого-то вручную после запуска бота?**
A: Обновите whitelist (удалите файл и перезапустите бота).

**Q: Можно ли защитить конкретных людей?**
A: ДА! Добавьте их user_id в раздел `custom` в `whitelist.json`.

**Q: Whitelist замедляет бота?**
A: Нет, проверка мгновенная (O(1) lookup в set).

## 🎯 Рекомендации

1. **Не удаляйте whitelist.json** - это ваша защита
2. **Делайте бэкап** whitelist.json периодически
3. **Обновляйте раз в месяц** если активно растете
4. **Проверяйте логи** - бот сообщает когда пропускает whitelist

## 📝 Пример логов

```
🔍 Finding users from @competitor...
✅ Found 50 potential targets (excluded 10 whitelisted)

▶️ Starting follow campaign...
✅ Followed @user1
✅ Followed @user2
🛡️ User 123456789 is whitelisted, skipping...
✅ Followed @user3
...

🔄 Unfollowing users who didn't follow back...
🛡️ User 987654321 is whitelisted, NEVER unfollowing!
➖ Unfollowed @inactive_user
...
```

---

**Ваши существующие связи полностью защищены! 🛡️**
