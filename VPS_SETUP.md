# 🚀 Полная инструкция по развертыванию на VPS

Пошаговая инструкция для запуска Instagram бота на VPS с управлением через Telegram.

---

## 📋 Что вы получите:

- ✅ Бот работает 24/7 на VPS
- ✅ Полное управление через Telegram
- ✅ Автоматический режим (запуск по расписанию)
- ✅ Статистика в реальном времени
- ✅ Автоперезапуск при сбоях

---

## 🎯 Шаг 1: Получить бесплатный VPS

### Вариант A: Oracle Cloud (БЕСПЛАТНО НАВСЕГДА)

1. **Регистрация:**
   - Перейдите на https://cloud.oracle.com/
   - Нажмите "Start for free"
   - Заполните форму (нужна карта, но деньги НЕ спишутся)

2. **Создание VM:**
   - Войдите в консоль
   - Compute → Instances → Create Instance
   - **Image:** Ubuntu 22.04
   - **Shape:** VM.Standard.E2.1.Micro (Always Free)
   - **SSH Keys:** Создайте новую пару ключей (скачайте приватный ключ!)
   - Нажмите "Create"

3. **Получите IP адрес:**
   - После создания скопируйте Public IP

### Вариант B: Google Cloud ($300 на 3 месяца)

1. Перейдите на https://cloud.google.com/
2. Активируйте Free Trial
3. Создайте VM: Compute Engine → VM Instances → Create
4. Выберите: e2-micro, Ubuntu 22.04

### Вариант C: AWS Free Tier (12 месяцев)

1. Перейдите на https://aws.amazon.com/
2. Создайте аккаунт
3. EC2 → Launch Instance
4. Выберите: t2.micro, Ubuntu 22.04

---

## 🔐 Шаг 2: Подключиться к VPS

### Windows (PowerShell):

```powershell
# Если у вас есть SSH ключ
ssh -i путь\к\ключу.pem ubuntu@ВАШ_IP

# Если используете пароль
ssh ubuntu@ВАШ_IP
```

### Альтернатива: PuTTY
1. Скачайте PuTTY: https://www.putty.org/
2. Введите IP адрес
3. Connection → SSH → Auth → Browse (выберите ключ)
4. Open

---

## 📦 Шаг 3: Установка Docker на VPS

Подключитесь к VPS и выполните:

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Установка Docker Compose
sudo apt install docker-compose -y

# Добавление пользователя в группу docker
sudo usermod -aG docker $USER

# Перезагрузка (чтобы применились права)
sudo reboot
```

Подождите 1-2 минуты и подключитесь снова.

---

## 🤖 Шаг 4: Создать Telegram бота

1. **Откройте Telegram**
2. **Найдите @BotFather**
3. **Создайте бота:**
   ```
   /newbot
   ```
4. **Введите имя:** `My Instagram Bot`
5. **Введите username:** `my_instagram_bot` (должен быть уникальным)
6. **Скопируйте токен:** `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`

---

## 📤 Шаг 5: Загрузить файлы на VPS

### Вариант A: Через SCP (с вашего компьютера)

```powershell
# Перейдите в папку с ботом
cd C:\Users\USER\Desktop\Instagram_Booster

# Загрузите файлы на VPS
scp -i путь\к\ключу.pem *.py *.txt Dockerfile docker-compose.yml ubuntu@ВАШ_IP:~/instagram-bot/
```

### Вариант B: Через Git (проще)

На VPS выполните:

```bash
# Создайте директорию
mkdir ~/instagram-bot
cd ~/instagram-bot

# Скопируйте файлы вручную (см. ниже)
```

Затем создайте файлы на VPS:

```bash
# Создайте каждый файл
nano instagram_follower_bot.py
# Вставьте содержимое, Ctrl+X, Y, Enter

nano telegram_control_bot.py
# Вставьте содержимое, Ctrl+X, Y, Enter

nano Dockerfile
# Вставьте содержимое, Ctrl+X, Y, Enter

nano docker-compose.yml
# Вставьте содержимое, Ctrl+X, Y, Enter

nano requirements_bot.txt
# Вставьте содержимое, Ctrl+X, Y, Enter
```

---

## ⚙️ Шаг 6: Настройка бота

На VPS:

```bash
cd ~/instagram-bot

# Отредактируйте telegram_control_bot.py
nano telegram_control_bot.py
```

Найдите строку:
```python
TELEGRAM_TOKEN = "YOUR_TELEGRAM_BOT_TOKEN"
```

Замените на ваш токен от BotFather:
```python
TELEGRAM_TOKEN = "123456789:ABCdefGHIjklMNOpqrsTUVwxyz"
```

Сохраните: `Ctrl+X`, `Y`, `Enter`

---

## 🚀 Шаг 7: Запуск бота

```bash
cd ~/instagram-bot

# Сборка Docker образа
docker-compose build

# Запуск бота
docker-compose up -d

# Проверка логов
docker-compose logs -f
```

Вы должны увидеть:
```
🤖 Telegram bot started!
```

Нажмите `Ctrl+C` для выхода из логов (бот продолжит работать).

---

## 📱 Шаг 8: Управление через Telegram

1. **Откройте Telegram**
2. **Найдите вашего бота** (по username, который вы создали)
3. **Нажмите START**
4. **Настройте бота:**
   - ⚙️ Настройки → 🔐 Instagram логин
   - Введите username и password от Instagram
   - ⚙️ Настройки → 🎯 Целевая аудитория
   - Добавьте конкурентов или хештеги
   - ⚙️ Настройки → ⚙️ Режим работы
   - Выберите режим (рекомендуется Умеренный)

5. **Запустите бота:**
   - ▶️ Запустить

6. **Включите авто-режим** (опционально):
   - ⚙️ Настройки → 🤖 Авто-режим
   - Бот будет работать автоматически 24/7

---

## 📊 Команды управления через Telegram

| Кнопка | Действие |
|--------|----------|
| 📊 Статус | Проверить состояние бота |
| ▶️ Запустить | Запустить одну сессию |
| ⏸️ Остановить | Остановить бота |
| ⚙️ Настройки | Изменить параметры |
| 📈 Статистика | Посмотреть результаты |

---

## 🔧 Полезные команды VPS

### Проверить статус:
```bash
docker-compose ps
```

### Посмотреть логи:
```bash
docker-compose logs -f
```

### Перезапустить бота:
```bash
docker-compose restart
```

### Остановить бота:
```bash
docker-compose down
```

### Обновить код:
```bash
# Остановить
docker-compose down

# Обновить файлы (загрузите новые версии)

# Пересобрать и запустить
docker-compose build
docker-compose up -d
```

---

## 🛡️ Безопасность

### Настройка Firewall:

```bash
# Разрешить только SSH
sudo ufw allow 22/tcp
sudo ufw enable
```

### Автоматические обновления:

```bash
sudo apt install unattended-upgrades -y
sudo dpkg-reconfigure -plow unattended-upgrades
```

---

## 🔄 Автоперезапуск при сбоях

Docker Compose уже настроен на автоперезапуск (`restart: unless-stopped`).

Если VPS перезагрузится, бот автоматически запустится.

---

## 📈 Мониторинг

### Проверка использования ресурсов:

```bash
# CPU и память
docker stats

# Дисковое пространство
df -h
```

---

## ❓ Troubleshooting

### Бот не запускается:

```bash
# Проверьте логи
docker-compose logs

# Пересоберите контейнер
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

### Ошибка "Permission denied":

```bash
# Дайте права на файлы
chmod +x *.py
```

### Telegram бот не отвечает:

1. Проверьте токен в `telegram_control_bot.py`
2. Убедитесь, что бот запущен: `docker-compose ps`
3. Проверьте логи: `docker-compose logs`

### Instagram блокирует:

- Уменьшите агрессивность (используйте Safe режим)
- Увеличьте задержки между действиями
- Сделайте перерыв на 24-48 часов

---

## 💰 Стоимость

### Oracle Cloud Always Free:
- **0₽/месяц** навсегда
- 1 CPU, 1GB RAM
- 50GB диска

### Google Cloud Free Trial:
- **$300 кредитов** на 3 месяца
- Потом ~$5-10/месяц

### AWS Free Tier:
- **Бесплатно** 12 месяцев
- Потом ~$5-10/месяц

---

## 🎉 Готово!

Теперь ваш бот работает 24/7 на VPS и полностью управляется через Telegram!

### Рекомендуемый режим работы:

1. **Авто-режим:** Включен
2. **Режим:** Умеренный (50 подписок/сессия)
3. **Сессий в день:** 3 (09:00, 14:00, 19:00)
4. **Ожидаемый результат:** 15-45 новых подписчиков/день
5. **До 3000 подписчиков:** ~2-3 месяца

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `docker-compose logs`
2. Перезапустите: `docker-compose restart`
3. Проверьте настройки в Telegram боте
